# generate

The dispatcher. Takes a model name and forwards the remaining keyword arguments
to that model's generator unchanged.

See [Choosing a model](../models/index.md) for which name to pass, and the
[Generators](generators.md) page for each generator's own signature.

::: gen_surv.interface
